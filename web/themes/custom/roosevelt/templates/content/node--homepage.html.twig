{#
/**
 * @file
 * Theme override to display a homepage node.
 *
 * This file only displays the content variable since the default view_mode
 * uses Layout Builder. Use the node--homepage--VIEW_MODE naming convention
 * for all other view_modes.
 */
#}
{{- content -}}
